/*
#include <iostream>

using namespace std;

class Car {
public:
  string brand;
  string model;
  int year;
  Car(string x, string y, int z);
};

Car::Car(string x, string y, int z) {
  brand = x;
  model = y;
  year = z;
  cout << "You have a " << x << ", model: " << y << " made in " << z << endl;
}

int main(void) {
  Car bmw("BMW", "X5", 1999);
  Car tesla("Tesla", "X", 2004);
  return 0;
}
 */